{
  "name": "estyi-backend",
  "version": "1.0.0",
  "description": "Estyi Platform Backend API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "healthcheck": "curl -f http://localhost:$PORT/health || exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.53.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "nodemailer": "^6.9.7",
    "vite-react-typescript-starter": "file:.."
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
